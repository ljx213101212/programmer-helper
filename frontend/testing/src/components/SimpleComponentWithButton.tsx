import React from "react";

export default (props) => {

    return (

        <div>
            <p>Simple Function Component 2</p>
            <button onClick={() => props.onButtonClick()}></button>
        </div>

    )

}